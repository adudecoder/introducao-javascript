// Módulo (%)
// Operador binário. Retorna o inteiro restante da divisão dos dois operandos.

12 % 5 // Retorna 2.

// Incremento (++) e Decremento (--)
++X
X++

const A = ++2; // Já incrementa, 3
const B = 2++; // Vai receber apenas o valor atual, 2

--X
X--

// Negação (-) e Adição (+)
-3
+"3"    // Retorna 3
+true   // Retorna 1
+false  // retorna 0
-true   // Retorna -1

// Operador de exponenciação (**)
2 ** 3      // Retorna 8
10 ** -1    // Retorna 0.1

// Operador de agrupamento ()
2 * (3 + 2)

/**
* Operador binário
    operando1 operador operando2
    1 + 2

  Operador unário
    operando1 operador
    operador operando1
    ++X
    X++
    --X
    X--
*/